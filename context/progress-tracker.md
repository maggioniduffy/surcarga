# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

Phase 0 — scaffolding. No product features (search, matching, payments, map, notifications) implemented yet; those are separate units per `ai-workflow-rules.md`.

## Current Goal

Get a working Next.js + TypeScript + Tailwind + shadcn/ui project with the base Prisma data model in place and the location catalog seeded, so feature units can start on solid ground.

## Completed

- Confirmed actual installed versions against `node_modules` (not assumed): Next.js 16.2.12 (App Router, `src/` dir, Turbopack), Tailwind CSS v4.3.3, TypeScript, Prisma 7.9.1. See [[decision-stack-versions]].
- Confirmed `proxy.ts` is the current Next.js 16 convention (not `middleware.ts`) by reading `node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md` — no `proxy.ts` written yet since there's no auth logic to route yet.
- Applied the dark-only theme direction from `ui-context.md` to `src/app/globals.css` and `src/app/layout.tsx`: brand orange (`#FF5A1F`/`#FF7038`), status color tokens (published/confirmed/pending/urgent/featured, route density), Archivo (`--font-display`) + IBM Plex Sans (`--font-body`) via `next/font/google`, `lang="es"`, `dark` class always applied (no toggle).
- Installed and initialized shadcn/ui (`components.json`, style `base-nova`, `components/ui/button.tsx` generated). Wired its generated font/color variables into the project's own token names rather than leaving duplicate Geist fonts.
- Created the folder structure from `code-standards.md` File Organization: `app/(public)` (holds the landing page, moved from `app/page.tsx`), `app/(auth)`, `app/(dashboard)`, `app/api`, `lib/services`, `lib/db`, `components/ui`, `content` (empty dirs hold `.gitkeep`).
- Wrote `prisma/schema.prisma` with the six base entities (`User`, `Ubicacion`, `Viaje`, `Carga`, `Postulacion`, `Payment`) per `architecture-context.md` — data model only, no business logic. Required moving to Prisma 7's driver-adapter pattern; see [[decision-prisma7-driver-adapter]]. A few structural fields/enums weren't literally specified and are flagged in [[decision-schema-assumptions]] and below.
- Created `src/lib/db/client.ts` (Prisma Client singleton with `@prisma/adapter-pg`) and `prisma/seed.ts` (idempotent `upsert` seed for the locations catalog), wired via `prisma.config.ts` `migrations.seed`.
- Verified end-to-end against a throwaway local Postgres+PostGIS Docker container (user-approved, then torn down): `prisma migrate dev` created and applied `prisma/migrations/20260727223516_init`, `prisma db seed` inserted all 9 base cities and Loma Campana correctly. `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass.

- Built the public landing page as a port of a delivered Claude Design artifact — see [[decision-landing-design-source]]. Eleven sections in `src/components/landing/`: header, hero (contour-line background + glow + stat row), problema, cómo funciona (two role columns), funcionalidades grid, mapa de cobertura (schematic SVG + listing/corridor side panel), testimonios, precios, FAQ, CTA final, footer. All copy lives in `src/content/es/landing.ts`; the FAQ accordion is the only `'use client'` component.
- Extended the design tokens in `src/app/globals.css` to the artifact's full palette: surface scale (`--color-surface-base` `#0a0a0f` → `panel`/`sunken`/`raised`/`control` + warm variants), border scale (`--color-line-faint` → `-control-hover`, plus warm), ink scale (`--color-ink` `#e8e8ee` → `-ghost`), `--color-topo-line`, and a `routeDash` keyframe (respects `prefers-reduced-motion`). Status and route-density tokens were re-pointed at the artifact's exact values (green `#2ecc71`, yellow `#f0c419`, orange `#FF5A1F`). shadcn's `--background`/`--foreground`/`--card`/`--border`/`--muted-foreground` now alias these instead of holding their own greys.
- Created the `memory/` directory that `AGENTS.md` and `progress-tracker.md` both referenced but which never existed. The three memory files that were dangling (`decision-stack-versions`, `decision-prisma7-driver-adapter`, `decision-schema-assumptions`) have since been written; `memory/MEMORY.md` now indexes all twelve.
- Verified: `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass. Confirmed every custom token compiles to a real utility class in the built CSS (a mistyped `@theme` name would fail silently), and confirmed all eleven sections plus the five FAQ buttons render in the SSR output. No headless-browser tooling is installed in this environment, so the page has not been visually screenshotted.

- Resolved two of the landing copy's scope conflicts in `src/content/es/landing.ts` (2026-07-27): **geolocation tracking permanently deleted** (feature card + its `tracking` glyph, pricing perk, offline/satellite FAQ, "seguís la carga" step; map section reframed from "tiempo real / camiones disponibles ahora" to published viajes) and the **AI matcher deferred** ("La IA matchea" → the transportistas on that route see it; matching/score language → search and filters). Added a "Fee fijo por publicación" feature card so the grid still fills six cells. `npx eslint src` and `npx tsc --noEmit` pass. See [[decision-no-geolocation-tracking]] and [[decision-ai-matcher-deferred]]; `project-overview.md` Out Of Scope updated.

- Repaired the context/memory files after a bad write (2026-07-28): `context/architecture-context.md` had been overwritten with a stale copy of this tracker, and the `memory/` directory had been renamed to `memory ` (trailing space), which git saw as a full delete + re-add. The architecture file was recovered by the user (a fuller version than git HEAD, with the Clerk Third-Party Auth detail and the Prisma-bypasses-RLS reasoning spelled out) and is the source of truth. Reconciled everything else to it: `code-standards.md`, invariant 5 and the Location Catalog section (which still claimed RLS was the enforcement layer), [[decision-role-model]], [[decision-locations-catalog]], [[decision-stack-assumptions]], and Supabase's status as confirmed rather than assumed. Merged the two duplicated indexes in `memory/MEMORY.md` into one.

- Connected the Supabase JS client (2026-07-28): installed `@supabase/supabase-js` 2.110.9 + `@supabase/ssr` 0.12.3, added `.env.local` with `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` for project `knyudvcireraowxxqwnd`, and created `src/lib/supabase/{client,server,proxy}.ts`. Placed under `src/lib/` (not a top-level `utils/`) to match the File Organization standard. `proxy.ts` exports an unused `updateSession` helper — Clerk owns the session lifecycle, so no `proxy.ts` at the app root calls it; see [[decision-supabase-client-setup]]. `npx tsc --noEmit` and `npx eslint src/lib/supabase` pass.

- Installed and wired Clerk (2026-07-28): `clerk init` linked the repo to the Clerk app **"Sur Carga"** (`app_3H8dGU87HriHzGYnxZTtPct23lh`, development instance; production not configured) and installed `@clerk/nextjs` 7.6.2 + `@clerk/ui`. `src/proxy.ts` now runs `clerkMiddleware()` with `/__clerk/:path*` appended to the matcher; the CLI-scaffolded sign-in/sign-up catch-all routes were moved from the app root into the documented `app/(auth)/` group (route groups don't change URLs, so `/sign-in` and `/sign-up` still match the `NEXT_PUBLIC_CLERK_SIGN_{IN,UP}_URL` env values). `ClerkProvider` sits inside `<body>` in `app/layout.tsx` with the `shadcn` appearance theme, which picks up the project's dark tokens for free since `globals.css` already maps the shadcn CSS variables onto them. `site-header.tsx` now shows Ingresar / Soy transportista / Necesito enviar carga as Clerk sign-in and sign-up triggers when signed out, and a `UserButton` when signed in. `clerk doctor`, `npx tsc --noEmit`, and a dev-server smoke test of `/`, `/sign-in`, `/sign-up` all pass. See [[decision-clerk-nextjs-integration]].

- Built the five app screens as ports of the delivered Claude Design artifacts (2026-07-28) — see [[decision-app-screens-design-source]]. Routes: `app/(dashboard)/dashboard`, `app/(dashboard)/publish-cargo`, `app/(dashboard)/publish-trip` (behind the guard), plus `app/(public)/search-trucks` and `app/(public)/trips/[id]` since published listings are public-readable browse content per `architecture-context.md` System Boundaries. Presentation only — no server actions, no data layer, no Zod schemas; every figure is placeholder sample data from the artifacts.
- Added the **Server Layout Guard** at `app/(dashboard)/layout.tsx`. It only verifies that a Clerk session exists (`auth()` → redirect to `/sign-in`, confirmed returning 307 when signed out); role gating and the onboarding redirect still wait on the Clerk webhook and a readable `users.role`.
- Split `src/content/es.ts` into `src/content/es/` (`brand`, `landing`, `app-shell`, `dashboard`, `publish-cargo`, `publish-trip`, `search-trucks`, `trip-detail`) with an `index.ts` barrel, so `@/content/es` imports keep working. Added `src/lib/routes.ts` so nav copy never hardcodes a path.
- New shared layers: `src/components/app-shell/` (`AppHeader` with a hamburger nav below `lg`, `AppFooter`, `UserChip`), `src/components/common/` (`CardPanel`/`CardEyebrow`/`CardHeader`, `ActionButton`/`ActionLink`/`InlineLink`, `StatusBadge`/`TagPill`/`DensityDot`, `RouteArrow`, `FormSteps`/`PageIntro`/`IntroBadge`, `CheckIcon`/`AlertIcon`/`WarningNote`) and `src/components/forms/` (`Field`, `TextField`/`TextareaField`, `SelectField`, `CheckboxField`, `ChipToggle`, `RangeField`) composed on top of newly generated shadcn primitives (`input`, `label`, `textarea`, `checkbox`, `select`, `slider`, `badge` — Base UI based, matching the `base-nova` style).
- **Made responsive** (the artifacts were fixed-width canvases): every `1fr / 3xx px` split collapses to one column with the aside sticky only from `lg`; 4-up stat and metric grids step 1 → 2 → 4; the dashboard listing tables become labelled stacks below `md`; the Buscar Camiones filter rail becomes a collapsible panel below `lg` and the result cards' right rail moves under the body; the region map SVG scales with its container instead of a fixed 760px canvas; headlines use `clamp()`.
- Verified: `npx tsc --noEmit`, `npx eslint src` and `npm run build` all pass. Confirmed all 35 custom design tokens used by these screens compile to real utilities in the built CSS (a mistyped `@theme` name fails silently). Rendered every screen at 1280–1440px and 390px with headless Chrome and checked the output against the artifacts — no horizontal overflow at 390px, and the fixes that came out of that pass (stacked-table label crowding, `·`/`—` separators starting a wrapped line, clipped date inputs in the 272px filter rail) are applied.

- Renamed everything the five screens introduced to English after the "ALL CODE AND FILES MUST BE WRITTEN IN ENGLISH" rule was added to `code-standards.md` (2026-07-28). Route segments (`/dashboard`, `/publish-cargo`, `/publish-trip`, `/search-trucks`, `/trips/[id]`), directories (`components/cargo`, `components/trip`, `components/search`), file names, component and export names (`PublishCargoView`, `TripResultCard`, `CarrierPanel`/`ShipperPanel`, `publishCargo`/`publishTrip`/`searchTrucks`/`tripDetail`), DOM ids (`cargo-*`, `trip-*`, `filter-*`) and every enum-ish `id`/`value` slug in the content files. The Spanish `label`/`title`/`body` strings are untouched — they are UI copy. Also fixed the same pre-existing violation in the landing code (`howItWorks.carrier`/`.shipper`, `pricing.carrierBanner`, `ctaCarrier`/`ctaShipper`) and updated the `components/{domain}/` example in `code-standards.md`, which still showed `components/carga/carga-card.tsx`.

- Renamed the Prisma schema to English (2026-07-28), the user's call on the open question above — see [[decision-english-domain-naming]]. `prisma/migrations/20260728000000_rename_domain_to_english/migration.sql` is hand-written as `ALTER ... RENAME` for the tables, the `trip_id`/`cargo_id` columns, the `Role` and `LocationType` enum values, the two enum type names, and every PK/unique/FK constraint name, because `migrate diff` would have emitted DROP + CREATE and dropped the data. `prisma/seed.ts` now writes `prisma.location` with `type: "city" | "oilfield"`.
- Verified the migration against a throwaway local Postgres+PostGIS Docker container (torn down afterwards), both paths: **upgrade** — applied the original init migration, inserted a row into all six tables using the old Spanish enum values, applied the rename, and confirmed all six rows survived with `role` now `carrier`/`shipper` and `type` now `city`/`oilfield`; **fresh install** — `prisma migrate deploy` applied both migrations from zero and `prisma db seed` inserted the 9 cities + Loma Campana. On both databases `prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma` reported **"No difference detected"**, so the migrated database matches the schema exactly with no drift. `.env` still holds the placeholder `DATABASE_URL` — nothing was pointed at a real database.

- Opened access to the dashboard pages from the public site (2026-07-28). The TODO in `site-header.tsx` is resolved: signed-in visitors now get an "Ir al panel" CTA (`landing.header.ctaDashboard`, linked through `routes.dashboard`) next to the `UserButton`, where signed-out visitors get the Ingresar / sign-up triggers. Post-auth landing moved from `/` to `/dashboard` via `NEXT_PUBLIC_CLERK_SIGN_{IN,UP}_FALLBACK_REDIRECT_URL` in `.env.local` (local-only file — the same two values need setting in the Vercel environment before deploy). `/publish-cargo` and `/publish-trip` are reachable from the `AppHeader` nav once inside. Verified with a dev-server smoke test: signed out, `/` renders no "Ir al panel" and `/dashboard` still returns 307 → `/sign-in`. `npx tsc --noEmit` and `npx eslint src` pass.

- **Added the light theme and removed the ES/EN toggle (2026-07-28)** — see [[decision-light-theme]]. Both were reversals of earlier decisions, at the user's direction: `ui-context.md` had said dark-only/no toggle, and [[decision-app-screens-design-source]] had listed the artifacts' theme switch as deliberately not carried over. The light palette is imported from the same Claude Design project (`2d54966f-…`), whose seven `.dc.html` files all carry an identical `#app-root[data-theme="light"]` token block.
  - `globals.css` is now split into three groups: **fixed** tokens (brand, `--color-on-brand`, warm surfaces/borders, status, route), **themed** scales (surface/line/ink/topo — light in `:root`, dark in `.dark`), and the shadcn aliases, which now point at the themed scales in both directions so Clerk's `shadcn` appearance theme follows along for free.
  - `.dark, .on-dark-panel` share one rule body. Accent panels the design pins dark (warm, warning, published, the featured pricing/tier cards, both dark→light gradient banners, `WarningNote`) carry `on-dark-panel`, which re-declares the dark scale locally so nested ink/border utilities stay legible on a light page.
  - **Follow-up fix**, after the user reported dark-on-dark text in those panels: re-declaring the scales was not sufficient. `on-dark-panel` also has to (a) re-declare the shadcn aliases, which are declared on `:root` and — since custom properties resolve where they are declared — had already computed against the light scale and don't recompute in a nested block, and (b) set `color` directly, because text carrying no colour utility (`<dd className="m-0 text-right">`, the fee figure, "Total a pagar hoy") inherits from `<body>`. `.dark` never hits either problem because it sits on `<html>`, the same element as `:root`. Also pinned `FreeCard`, whose dark gradient lives in an inline `style` and so was missed by the first class-based sweep.
  - Added `--color-on-brand` / `-hover` and replaced all 12 `text-surface-base` usages plus the CTA block's `bg-`/`border-surface-base`: that token was doing double duty as "near-black ink on orange", which breaks the moment the canvas flips to off-white.
  - No-flash handling follows `next/dist/docs/01-app/02-guides/preventing-flash-before-hydration.md`: `themeInitScript` (`src/lib/theme.ts`) runs synchronously in `<head>`, `<html>` gets `suppressHydrationWarning`, and `ThemeProvider`'s lazy initializer reads the class the script set, so state and DOM always agree.
  - New: `src/lib/theme.ts`, `src/components/theme/{theme-provider,theme-toggle}.tsx`, `appShell.theme` copy. `landing.header.locales` deleted.
  - Verified: `npm run build`, `npx tsc --noEmit` and `npx eslint src` pass. Confirmed in the compiled CSS that `:root` carries the light values, `.dark, .on-dark-panel` carries the dark ones, and the `on-brand` utilities emit — and that the unlayered `:root` block wins over `@theme inline`'s self-referential `--color-x: var(--color-x)` declarations. Confirmed in the SSR output that the init script is inlined in `<head>`, the theme toggle renders with its Spanish `aria-label`, and no ES/EN markup remains. Two genuine light-mode breakages were caught by auditing fixed dark fills against themed ink and fixed (`WarningNote`, the pricing banner's dark→light gradient). Still not visually screenshotted — no headless-browser tooling in this environment.

## In Progress

Nothing actively in progress.

## Next Up

- Resolve the landing copy's scope conflicts before the site is public — see Open Questions below and [[open-landing-copy-scope-conflicts]].
- Build the role-selection onboarding step at `app/(auth)/onboarding/` — Clerk creates the identity without a role, so the Server Layout Guard needs somewhere to send users whose `users` row has no `role` yet.
- Add the Clerk webhook that syncs identity into the `users` table, and the Clerk↔Supabase Third-Party Auth connection (Clerk SDK itself is now installed).
- Replace the placeholder sample data on the five app screens with real queries, and turn the forms into Server Actions with Zod validation at the boundary. The screens are presentation-only today: the publishing forms don't submit, the search filters don't filter, and the org name/avatar in `AppHeader` come from content files rather than the session.
- Drop the role tabs from the dashboard header once `users.role` gates which panel renders — they are a preview affordance carried over from the design artifact, not a product feature. The landing page's in-body CTAs still point at in-page anchors — only the header CTAs are wired to Clerk.
- Configure a Clerk **production** instance before launch; only the development instance exists today.
- Pick the next unit per `ai-workflow-rules.md` scoping rules (e.g. auth/role signup flow, or the ubicaciones "add inline while publishing" flow) — needs a user decision on priority.
- Provision the real Supabase Postgres+PostGIS database and point `DATABASE_URL`/`DIRECT_URL` at it; `.env` currently holds the original placeholder connection string.
- ~~Clerk app + Next.js SDK~~ — done, see Completed above. Still outstanding before the `app/(dashboard)/layout.tsx` Server Layout Guard: the Clerk↔Supabase Third-Party Auth connection and the Clerk webhook that syncs identity into the `users` table.

## Open Questions
- ~~The English-code rule collides with the Spanish domain model~~ — **Resolved 2026-07-28:** the user chose to rename the schema. `prisma/migrations/20260728000000_rename_domain_to_english/` renames the tables, columns and enum values in place; the UI and data layers now share one vocabulary (`carrier`/`shipper`, `Trip`, `Cargo`, `Location`, `Application`). See [[decision-english-domain-naming]].
- **The landing page advertises features and figures the context files don't back** — ported verbatim from the design artifact at the user's direction, flagged rather than softened. Two of them are now resolved (tracking deleted, AI matcher deferred — see below). See [[open-landing-copy-scope-conflicts]] for what remains: USD 19 / USD 39 listing fees (fee amount and currency are still open, and ARS was the presumed currency), transportista verification claims (CUIT/RUTA/titularidad — nothing in the schema models this), and invented stats (38%, 1.240 viajes/mes) plus three named fictional testimonials. None of this is a requirements source; all of it must be confirmed or replaced before launch.
- ORM: Prisma was assumed over Drizzle in architecture-context.md — confirm.
- ~~Database host~~ — **Resolved:** Supabase (Postgres + PostGIS), confirmed in `architecture-context.md`; chosen for bundled Storage and geospatial-ready Postgres. Note the original rationale ("bundles Auth") no longer applies now that auth is Clerk.
- ~~Auth provider~~ — **Resolved:** Clerk, confirmed by the user, connected via Supabase's native Third-Party Auth. See [[decision-auth-clerk]].
- ~~File/object storage provider~~ — **Resolved:** Supabase Storage, confirmed by the user (explicitly not Amazon/S3).
- Whether RLS policies actually get written for `locations` and the Storage buckets as defense-in-depth, or whether app-level checks alone are judged sufficient given Prisma bypasses RLS on the main data path — revisit once the Clerk↔Supabase webhook sync is implemented.
- Map library: MapLibre GL JS was assumed over Leaflet — confirm.
- Email fallback provider: Resend vs. Postmark not yet chosen.
- Exact fixed listing fee amount and currency (ARS presumably) for a carga publication — not specified in the source description.
- Stripe fallback: scope and trigger conditions undefined; not to be implemented until explicitly requested.
- Seed list confirmed for the base cities (Rincón de los Sauces, Cutral Có, Neuquén, Añelo, Centenario, Cipolletti, Catriel, El Chañar, Plottier) and yacimientos like Loma Campana; the catalog is otherwise extensible by any authenticated user at publish time. Still open: the fuller starting list of yacimientos/bases/campamentos beyond Loma Campana, if one exists, so the catalog doesn't launch nearly empty on the yacimiento side.
- How `admin` accounts are created/granted (no public self-signup implied) — manual DB grant, a separate invite flow, or something else — not yet specified.
- Whether new user-added ubicaciones need any lightweight duplicate/typo check before insert (e.g. "Loma Campana" vs "Loma la Campana") or whether that's left entirely to admin cleanup after the fact.
- ~~Exact Next.js, Tailwind, and Prisma versions to pin~~ — resolved, see [[decision-stack-versions]]: Next.js 16.2.12, Tailwind v4.3.3, Prisma 7.9.1.
- `ApplicationStatus` values (`pending`/`confirmed`/`rejected`) were invented for the initial schema — architecture-context.md's Matching & Postulación Model doesn't specify a state machine, only that a record is created and a notification fires. Needs confirmation before the postulación flow is built. See [[decision-schema-assumptions]].
- `CargoStatus` values beyond `pending_payment`/`published` (`confirmed`, `completed`) were inferred from the Core User Flow narrative, not stated as an explicit enum in architecture-context.md. Needs confirmation. See [[decision-schema-assumptions]].
- The "Destacada" (featured) badge in `ui-context.md`'s status color system has no backing field or business rule anywhere in `architecture-context.md` or `project-overview.md` — unclear if it's a paid upsell, an algorithmic flag, or purely presentational. Not modeled in the schema.
- `Location.type` (`city` vs. `oilfield`) and the `Application` requirement that both `tripId` and `cargoId` be present (no support yet for the "direct contact" case mentioned in project-overview.md) are both schema-level assumptions — see [[decision-schema-assumptions]].
- No real Postgres/PostGIS database is provisioned yet — `.env` holds a placeholder `DATABASE_URL`; schema/migration/seed were only verified against a throwaway local Docker container that was torn down afterward.

## Architecture Decisions
- **Confirmed:** Supabase for Postgres+PostGIS and Storage, over Neon — chosen for bundled Storage alongside a geospatial-ready Postgres.
- **Confirmed:** Auth is Clerk (not Supabase Auth), connected via Supabase's native Third-Party Auth; Storage is Supabase Storage (not Amazon/S3). Because Prisma connects to Postgres directly and bypasses Clerk-JWT-based RLS, app-level session/role/ownership checks are the primary authorization control for viajes, cargas, ubicaciones, and postulaciones — RLS is defense-in-depth, not the enforcement layer, for anything queried through Prisma. See [[decision-auth-clerk]].
- Assumed Prisma as the ORM over Drizzle, for migration tooling and ecosystem maturity; flagged for confirmation.
- Assumed MapLibre GL JS over Leaflet for the map, for better vector-tile styling flexibility matching the dark/orange theme; flagged for confirmation.
- Theme is **dark-first with an opt-in light theme** (revised 2026-07-28; was dark-only). Dark remains the default and does not follow `prefers-color-scheme`. Brand, warm and status colors are fixed across both themes — only the surface/line/ink scales flip. See [[decision-light-theme]].
- Role (`carrier` | `shipper`) is stored as a single enum column on `users`, set once at signup and treated as immutable.
- Confirmed as a modular monolith inside a single Next.js project, organized by domain — no microservices at this stage.
- Fixed fee per carga listing, paid before the listing becomes searchable; drivers never pay to publish or contact; no commission taken on freight.
- Auth gating for the dashboard route group is placed in a Server Layout Guard (`app/(dashboard)/layout.tsx`), not in `proxy.ts` — see Session Notes below for why.
- Added a narrow `admin` role (third value on `users.role`) whose only granted capability is editing/deleting locations catalog entries — not a general back-office role. The locations catalog is open-write (any authenticated user can add a missing location while publishing) but admin-only for edit/delete, enforced in the server action before the Prisma write and mirrored in RLS as defense-in-depth.

## Session Notes
Generated 2026-07-27. Framework/tooling notes to verify against the actual installed versions before scaffolding:
- **Next.js 16** renamed `middleware.ts` to `proxy.ts`; the framework's own guidance is that `proxy.ts` handles routing-boundary concerns (rewrites, redirects, headers) and should not hold authentication/session logic — auth checks belong in Server Components/layouts closer to the data layer (a "Server Layout Guard" pattern). This was a deliberate response to real middleware-based auth-bypass vulnerabilities in earlier versions. Confirm the installed Next.js version actually matches this convention before writing routing/auth code.
- **Tailwind CSS v4** replaces `tailwind.config.js` with CSS-first configuration via the `@theme` directive directly in the global stylesheet. If v4 is confirmed as the installed version, design tokens (colors, fonts, radii) should be defined there, not in a JS config file.
- Six screens were referenced from the provided design export: Landing, Buscar Camiones (search/browse), Detalle Viaje (trip detail), Publicar Carga, Publicar Viaje, Dashboard. Observed and carried into `ui-context.md`: brand orange `#FF5A1F` (hover `#FF7038`), Archivo for display type, IBM Plex Sans for body, radii clustering at 6–10px (controls) and 12–16px (cards), and status badges for Publicado / Urgente / Destacada.
